const cartReducer = (state, action) => {
	switch (action.type) {
    case 'ADD_ITEM':
      // check if item is in cart
      if(!state.cart)
		default:
			return state;
	}
};

export default cartReducer;
